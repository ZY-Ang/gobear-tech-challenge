extends ../main

block title
  | #{note.name}

block content
  p.hidden-text Last edited #{note.updatedAt()}
  div.note
    p #{note.text}

  a.button(href='/express/notes/#{note.id}/edit') Edit
  a.delete-note(href='/express/notes/#{note.id}/delete') Delete it
