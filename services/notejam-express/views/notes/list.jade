extends ../main

block title
  | #{title}

block content
  if notes.length
    table.notes
      tr
        th.note
          | Note
          a.sort_arrow(href='/express/?order=-name')  ↑
          a.sort_arrow(href='/express/?order=name') ↓
        th
          | Pad
        th.date
          | Last modified
          a.sort_arrow(href='/express/?order=-updated_at')  ↑
          a.sort_arrow(href='/express/?order=updated_at') ↓
      each note in notes
        tr
          td
            a(href='/express/notes/#{note.id}') #{note.name}
          td.pad
            if note.pad
              a(href='/express/pads/#{note.pad.id}') #{note.pad.name}
            else
              | No pad
          td.hidden-text.date #{note.updatedAt()}
  else
    p.empty Create your first note
  a.button(href='/express/notes/create') New note

